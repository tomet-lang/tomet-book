[package]
name = "tmtbook-search"
version = "0.1.0"
edition = "2024"
description = "Lightweight search index data structures and serialization for tomet-book"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
