`sprite.svg` is vendored from the `lucide-static` npm package
(https://www.npmjs.com/package/lucide-static, built from
https://github.com/lucide-icons/lucide), ISC licensed -- see `LICENSE`.
It bundles every Lucide icon as one `<symbol id="...">` per icon; `id`
is the name `@doc.icon("<name>", pkg:"lucide")` refers to.

To update: re-download
`https://unpkg.com/lucide-static/sprite.svg` over this file.
