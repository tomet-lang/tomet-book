{
  "name": "tmtbook-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/tmtbook.js",
    "dev": "esbuild src/index.ts --bundle --watch --outfile=dist/tmtbook.js"
  }
}
